import { requireAdmin } from '@/lib/admin/auth'

export default async function AuditLogPage() {
  const { service } = await requireAdmin(); const { data: entries } = await service.from('admin_audit_log').select('*').order('created_at',{ ascending:false }).limit(250)
  return <main className="admin-main"><header className="admin-heading"><div><p className="eyebrow">Audit Log</p><h1>Administrative record</h1><p>Administrator, Agent, and system decisions leave a timestamped record. Agent model, prompt, evidence, confidence, and structured output remain in Agent Runs.</p></div></header><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{entries?.map((entry) => <tr key={entry.id}><td>{new Intl.DateTimeFormat('en-US',{ dateStyle:'medium',timeStyle:'medium' }).format(new Date(entry.created_at))}</td><td>{entry.actor_type}<br /><small>{entry.actor_id}</small></td><td>{entry.action}</td><td>{entry.entity_type}<br /><small>{entry.entity_id}</small></td><td><pre className="audit-json">{JSON.stringify(entry.details,null,2)}</pre></td></tr>)}</tbody></table>{!entries?.length ? <p className="admin-empty">No audited actions yet.</p> : null}</div></main>
}
