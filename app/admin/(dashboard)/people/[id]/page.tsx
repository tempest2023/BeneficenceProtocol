import { notFound } from 'next/navigation'
import { updatePerson } from '@/app/admin/actions'
import { requireAdmin } from '@/lib/admin/auth'

export default async function EditPersonPage({ params }: { params: Promise<{ id:string }> }) {
  const { id }=await params; const {service}=await requireAdmin(); const {data:person}=await service.from('people').select('*').eq('id',id).maybeSingle(); if(!person) notFound()
  return <main className="admin-main"><header className="admin-heading"><div><p className="eyebrow">People / Edit</p><h1>{person.display_name}</h1><p>Role type, Contributor association, nominating director, and effective date remain fixed provenance. Edit the public presentation here.</p></div></header><section className="admin-panel"><form className="admin-form" action={updatePerson}><input type="hidden" name="id" value={person.id}/><label>Display name<input name="display_name" defaultValue={person.display_name} required/></label><label>Role<input name="role" defaultValue={person.role} required/></label><label>Responsibilities<textarea name="responsibilities" defaultValue={person.responsibilities??''}/></label><label>Biography<textarea name="biography" defaultValue={person.biography??''}/></label><label>Region<input name="region" defaultValue={person.region??''}/></label><label>Current work<textarea name="current_work" defaultValue={person.current_work??''}/></label><label>Active since<input type="date" name="active_since" defaultValue={person.active_since??''}/></label><label>Website<input type="url" name="website_url" defaultValue={person.website_url??''}/></label><label>GitHub<input type="url" name="github_url" defaultValue={person.github_url??''}/></label><label>Google Scholar<input type="url" name="scholar_url" defaultValue={person.scholar_url??''}/></label><label>LinkedIn<input type="url" name="linkedin_url" defaultValue={person.linkedin_url??''}/></label><label>Manual order<input type="number" name="sort_order" defaultValue={person.sort_order}/></label><label>Replace photo<input type="file" name="image" accept="image/jpeg,image/png,image/webp"/></label><label>New photo alt text<input name="image_alt"/></label><label>New photo source<input name="image_source"/></label><label>New photo permission notes<textarea name="image_permission_notes"/></label><button>Save profile</button></form></section></main>
}
