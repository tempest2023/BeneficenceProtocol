import Link from 'next/link'
import { createResource, setResourcePublication } from '@/app/admin/actions'
import { requireAdmin } from '@/lib/admin/auth'
import { RESOURCE_FORMATS } from '@/lib/community/constants'

export default async function LearnAdminPage() {
  const { service } = await requireAdmin(); const { data: resources } = await service.from('resources').select('*').order('sort_order').order('created_at',{ ascending:false })
  return <main className="admin-main"><header className="admin-heading"><div><p className="eyebrow">Learn</p><h1>Public learning library</h1><p>Create and preview drafts, then publish, feature, manually order, or archive approved free resources.</p></div><Link className="admin-button admin-button--quiet" href="/community/learn" target="_blank">Open public Learn</Link></header><div className="admin-grid"><section className="admin-panel"><h2>Create resource</h2><form className="admin-form" action={createResource}><label>Title<input name="title" required /></label><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required /></label><label>Summary<textarea name="summary" required /></label><label>Public URL<input type="url" name="public_url" required /></label><label>Type<select name="resource_type">{RESOURCE_FORMATS.map((format) => <option key={format}>{format}</option>)}</select></label><label>Language<input name="language" required /></label><label>Difficulty<input name="difficulty" /></label><label>Topics (comma-separated)<input name="topics" /></label><label>Author or publisher<input name="author_publisher" /></label><label>Access notes<textarea name="access_notes" /></label><button>Create draft</button></form></section><section className="admin-panel"><h2>Publishing standard</h2><p>Only free, publicly accessible, approved resources may be published. Administrators must open the link and verify relevance, factual attribution, access, and copyright context; the Agent never visits URLs.</p></section></div><div className="admin-table-wrap" style={{ marginTop:'1rem' }}><table className="admin-table"><thead><tr><th>Resource</th><th>Source</th><th>State</th><th>Publish and order</th></tr></thead><tbody>{resources?.map((resource) => <tr key={resource.id}><td><strong>{resource.title}</strong><br /><small>{resource.resource_type} · {resource.language}</small><br/><Link href={`/admin/learn/${resource.id}`}>Edit</Link> · <Link href={`/admin/learn/preview/${resource.id}`}>Preview</Link></td><td><a href={resource.public_url} target="_blank" rel="noreferrer">Open URL ↗</a><br />{resource.author_publisher}</td><td><span className="status-badge">{resource.publication_status}</span></td><td><form className="admin-form" action={setResourcePublication}><input type="hidden" name="id" value={resource.id} /><label>Status<select name="status" defaultValue={resource.publication_status}><option value="draft">Draft / preview</option><option value="published">Published</option><option value="archived">Archived</option></select></label><label>Order<input type="number" name="sort_order" defaultValue={resource.sort_order} /></label><label><span><input type="checkbox" name="featured" defaultChecked={resource.featured} /> Featured</span></label><label><span><input type="checkbox" name="access_verified" defaultChecked={Boolean(resource.access_verified_at)} /> I opened this URL and verified that the resource is approved, free, and publicly accessible.</span></label><button>Save</button></form></td></tr>)}</tbody></table>{!resources?.length ? <p className="admin-empty">No resources yet. The public page will show its instructive empty state.</p> : null}</div></main>
}
